

    Prerequisites

* libcurl   7.18.0+  <http://curl.haxx.se/>
* libpcre   7.8.0+   <http://pcre.org/>
* libiconv  1.12.0+  <http://www.gnu.org/software/libiconv/>
* (lib)lua  5.1+     <http://lua.org/>

Behold!

 [libquvi]------\
       |   \     [libcurl]
   [liblua] \       \
          [libpcre]  \
                     [dragons be here]

  Illustr. #1: Demonstrating mad ASCII skillz.


    Installation

"./configure && make install", typically. See also "./configure --help".

For example:
    --enable-smut   Enable (install) adult website support
    --enable-broken Enable (install) broken website support
    --disable-quvi  Disable building and installation of the quvi program
    --without-man   Do not install the manual pages

Unless you are looking into fixing broken support, use of --enable-broken
is not recommended.

"make dox" generates the C API reference documentation from the doxygen
tags. Both, HTML and LaTeX, documentation are generated by default.


    Tests

e.g. "make check", the following environment variables can be used:

  NO_INTERNET         Disable all tests that require internet connection
  TEST_RESTRICTED     Enable website rule tests known for country restrictions
  QUVI_TEST_OPTIONS   Additional options to pass to quvi when the tests are run

Examples:
    QUVI_TEST_OPTIONS="--debug -n" ./make check
    NO_INTERNET=1 ./make distcheck


    Contributing

Please see the howto files in doc/quvi/ subdir. The most current TODO
file can be viewed at:
  http://repo.or.cz/w/quvi.git?a=blob_plain;f=TODO;hb=HEAD


