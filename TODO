
0.1.2
-----
* quvi_read_callback
* quvi_read_callback_default
* #define QUVI_READFUNC_ABORT 0x10000000 (returned from quvi_read_callback)
typedef size_t (*quvi_read_callback)(char *buffer,
                                      size_t size,
                                      size_t nitems,
                                      void *instream);
* we add new option
  - the functionality of this stays hidden to those using the current interface
  - bumping the interface to 1 seems like an overkill
  - instead, let's go with 0:2:1

When due
--------
* youtube "feather"
  http://youtube-global.blogspot.com/
  http://www.youtube.com/feather_beta

0.2.x?
------
* bindings for other languages (using swig?)
